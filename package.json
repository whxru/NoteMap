{
  "name": "NoteMap",
  "description": "A cross-platform application which uses mindmap to manage notes in an account of Evernote.",
  "version": "0.1.0",
  "main": "src/main.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production electron .",
    "test": "cross-env NODE_ENV=development electron ."
  },
  "dependencies": {
    "echarts": "^4.0.2",
    "evernote": "^2.0.5",
    "markdown-it": "^8.4.1"
  },
  "devDependencies": {
    "cross-env": "^5.1.3"
  }
}
